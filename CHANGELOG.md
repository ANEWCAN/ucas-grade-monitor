# 更新日志 / Changelog

本项目遵循语义化版本号。

## [2.6.0] - 2026-07-31

### 新增

- 前台常驻服务，在用户启用自动查询后维持后台运行状态；
- AlarmManager 单次唤醒闹钟与持久化 JobScheduler 双重调度；
- 距离下次自动查询的实时倒计时；
- 精确闹钟与电池优化状态提示及快捷设置入口；
- 应用被任务列表划掉后的自动恢复安排。

### 优化

- 三个页面统一适配系统状态栏安全区，顶部增加白色留白；
- 课程成绩字号与分数色块尺寸缩小；
- 重启、应用升级及进程重建后恢复下一次查询时间。

### 限制

- Android 系统“强行停止”会禁止应用接收闹钟、任务和广播，必须由用户重新打开应用；
- 未授予精确闹钟或电池优化豁免时，厂商省电策略仍可能延后查询。

## [2.5.0] - 2026-07-30

### 新增

- “成绩 / 查询 / 设置”三个独立页面；
- 按学期分组的课程卡片列表；
- 学位课黄色边框、淡黄色背景和标签；
- 极简 UCAS 文字元素自适应图标；
- 自动查询、通知策略、失败重试和查询记录。

### 修复

- 兼容验证码接口中对象、字符串及 `data:` 等响应形式；
- 修复登录 Cookie、SEP Session、JWXK Identity 跳转；
- 修复自动查询对 HTTP 422 stream timeout 的重试与系统退避处理。

## [2.4.0] - 2026-07-30

- 修复字符串响应被强制转换为 `JSONObject` 的错误；
- 将成绩页和设置页拆分；
- 更新 Android 自适应图标。

## [2.2.0] - 2026-07-30

- 对齐参考项目的 UCAS 邮箱身份格式、Cookie 和 JWXK 登录流程。
